https://codepen.io/ajwpdx/pen/ExPxjYV?editors=1100

<!-- Questions to Submit in today's file first-lastname.txt --> 
<!--
Copy the questions below into the first-lastname.txt file on your github repo - answer each question and then push your changes. 

    1. What is Semantic HTML? ** It how we describe the elements of HTML that give meaning to its content.  

    2. What is HTML used for? ** It is the basic information of a website. 

    3. What is an attribute and where do we put it? ** It is a word that helps define and control an element's behavior. It goes in the opening tag of an element.

    4. What is the h1 tag used for? ** How many times should I use it on a page? It is the primary heading. It should be used sparingly for only the most important info, perhaps for just the main title/heading on a page. 

    5. Name two tags that have required attributes ** An "<a>" tag requires "href" attribute. An "<input>" tag requires a "type" attribute.

    6. What do we put in the head of our HTML document? ** The title, styles, links to CSS, scripts, meta tags.

    7. What is an id? ** An attribute that specifies a unique element. It can be referenced using a "#" character.

    8. What elements can I add an id to? ** Any

    9. How many times can I use the same id on a page? ** Once.

    10. What is a class? ** An attribute that specifies a type of element that can be styled in CSS.

    11. What elements can I add a class to? ** Any

    12. How many times can I use the same class on a page? ** unlimited

    13. How do I get my link to open in a new tab? ** add the following attribution: target = "_blank"

    14. What is the alt attribute in the image tag used for? ** It describes the image in the case that it cannot be displayed. 

    15. How do I reference an id? ** with a "#" symbol

    16. What is the difference between a section and a div? ** A section is a semantic element that defines sections within the page. Div's are don't have a predetermined meaning.

    17. What is CSS used for? ** Styling HTML

    18. How to we select an element? Example - every h2 on the page ** In CSS, simply write the element followed by brackets "{}"

    19. What is the difference between a class and an id? - Give me an example of when I might use each one ** Class be used multiple times to apply the same style to multiple elements. ID references a specific element and can be used for navigation within the page to that element.
    
    20. How do we select classes in CSS? ** with a "." before the brackets

    21. How do we select a p element with a single class of “human””? ** As follows: .human p {

    22. What is a parent child selector? When would this be useful? 

    23. How do you select all links within a div with the class of sidebar? ** as follows: .sidebar a {

    24. What is a pseudo selector? ** Pseudo selectors are denoted with the ":" symbol and are dependent on certain conditions to be valid (e.g. hover).

    25. What do we use the change the spacing between lines? ** line-height:

    26. What do we use to change the spacing between letters? ** letter-spacing:  

    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize? ** text-transform:

    28. How do I add a 1px border around my div that is dotted and black? ** border: 1px dotted black

    29. How do I select everything on the page? ** "*" or body

    30. How do I write a comment in CSS? ** start with "/*" end with "*/"

    31. How do I find out what file I am in, when I am using the command line? ** pwd

    32. Using the command line - how do I see a list of files/folders in my current folder? ** ls

    33. How do I remove a file via the command line? Why do I have to be careful with this? ** rm , it is permanent.

    34. Why should I use version control? ** It lets you iterate without losing prior vesions of a program. 

    35. How often should I commit to github? often. ** 20 mins is recommended. 

    36. What is the command we would use to push our repo up to github? ** git push -u origin branch-name

    37. Walk me through Lambda's git flow. **
	1. Fork the file GitHub
	2. Add TL as collaborator in GitHub
	3. Clone the document by downloading the forked version (This makes a local replica). Type: git clone <link>.
	4. cd into the new repo using the cd <file> command
	5. create your own branch by typing the following: git checkout -b 'firstname-lastname'
	6. commit changes with three commands: 1) git add . 2) git commit -m "type comments here" 3) git push -u origin branch-name
	7. go into the project and submit a pull request for you TL. 

Stretch Questions

    1. What is the difference between an inline element and a block element?

    2. What happens when an element is positioned absolutely? 

    3. How do I make an element take up only the amount of space it needs but also have the ability to give it a width? 

    4. Name 3 elements that are diplay block by default, 2 elements that are display inline by default and 1 element that is display inline-block by default

    5. In your own words, explain the box model. What is the "fix" for the box model, in other words, how do we make all elements respect the width we've given them? 



   --> 